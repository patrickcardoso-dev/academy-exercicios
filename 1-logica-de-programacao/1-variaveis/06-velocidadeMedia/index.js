const distPercorrida = 500;
const tempo = 10;

const velocidade = distPercorrida / tempo;
const resultado = velocidade * 3.6;

const mensagem = `Sua velocidade média foi de ${resultado} km/h.`
console.log(mensagem);
