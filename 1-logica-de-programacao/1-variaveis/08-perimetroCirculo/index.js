const raio = 1;
const pi = 3.14;

const comprimento = 2 * pi * raio; 
const area = pi * (raio) ** 2;

const mensagem = `O comprimento do círculo é de ${comprimento} cm e a área é de ${area} cm2.`;
console.log(mensagem);
